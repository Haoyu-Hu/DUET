# DUET dependencies. Target: Python 3.12, CUDA cu128 wheels.
#
# torch + torchvision are installed first by setup/install.sh from the PyTorch
# CUDA wheel index (cu128). The lines below document the version pins; pip
# will see them already installed and skip. cu128 wheels are forward-
# compatible with CUDA 13 at runtime via PTX JIT.

# ── Core ML stack ───────────────────────────────────────────────────────────
torch==2.7.0
torchvision==0.22.0
torchdata>=0.10

# ── Transformers / inference ────────────────────────────────────────────────
transformers>=4.51,<5.0
tokenizers>=0.20
accelerate>=1.13
vllm>=0.9.0,<0.10

# DUET requires V0 vLLM (per-request LogitsProcessors are not supported under
# V1 in vLLM 0.9.2). The scripts/run_duet.sh wrapper exports VLLM_USE_V1=0;
# vLLM itself is the standard 0.9.x build.

# ── verl runtime ────────────────────────────────────────────────────────────
hydra-core==1.3.2
omegaconf>=2.3
ray==2.54.0
tensordict==0.6.2
dill==0.4.0
liger_kernel==0.7.0

# ── verl runtime support ────────────────────────────────────────────────────
codetiming>=1.4
einops>=0.8
cachetools>=5.0
msgspec>=0.18
pylatexenc>=2.10
mathruler>=0.1
sympy>=1.12
safetensors>=0.4
filelock>=3.12
regex>=2024.0
psutil>=5.9
pydantic>=2.0
aiohttp>=3.9
requests>=2.31
cloudpickle>=3.0

# ── LoRA ────────────────────────────────────────────────────────────────────
peft>=0.18,<0.20

# ── Data / I/O ──────────────────────────────────────────────────────────────
datasets>=4.0
huggingface_hub>=0.36
pandas>=2.2
pyarrow>=23
sentencepiece==0.2.1
protobuf>=4,<5

# ── Misc ────────────────────────────────────────────────────────────────────
numpy>=1.26,<2.0
matplotlib>=3.10
tqdm>=4.67
scipy>=1.13

# vllm 0.9.x requires setuptools>=77.0.3,<80; the vendored verl needs <81.
# Intersection: >=77.0.3,<80.
setuptools>=77.0.3,<80
wheel
